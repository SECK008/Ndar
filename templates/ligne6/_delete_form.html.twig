<form method="post" action="{{ path('app_ligne6_delete', {'id': ligne6.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne6.id) }}">
    <button class="btn">Delete</button>
</form>
