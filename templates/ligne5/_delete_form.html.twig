<form method="post" action="{{ path('app_ligne5_delete', {'id': ligne5.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne5.id) }}">
    <button class="btn">Delete</button>
</form>
