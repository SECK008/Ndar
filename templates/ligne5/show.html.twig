{% extends 'base.html.twig' %}

{% block title %}Ligne5{% endblock %}

{% block body %}
    <h1>Ligne5</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne5.id }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ ligne5.trajet }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ ligne5.disponibilite ? ligne5.disponibilite|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne5_index') }}">back to list</a>

    <a href="{{ path('app_ligne5_edit', {'id': ligne5.id}) }}">edit</a>

    {{ include('ligne5/_delete_form.html.twig') }}
{% endblock %}
