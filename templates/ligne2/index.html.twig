{% extends 'base.html.twig' %}

{% block title %}Ligne2 index{% endblock %}

{% block body %}
    <h1>Ligne2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Trajet</th>
                <th>Disponibilite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne2 in ligne2s %}
            <tr>
                <td>{{ ligne2.id }}</td>
                <td>{{ ligne2.trajet }}</td>
                <td>{{ ligne2.disponibilite ? ligne2.disponibilite|date('H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_ligne2_show', {'id': ligne2.id}) }}">show</a>
                    <a href="{{ path('app_ligne2_edit', {'id': ligne2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne2_new') }}">Create new</a>
{% endblock %}
