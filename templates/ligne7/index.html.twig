{% extends 'base.html.twig' %}

{% block title %}Ligne7 index{% endblock %}

{% block body %}
    <h1>Ligne7 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Trajet</th>
                <th>Disponibilite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne7 in ligne7s %}
            <tr>
                <td>{{ ligne7.id }}</td>
                <td>{{ ligne7.trajet }}</td>
                <td>{{ ligne7.disponibilite ? ligne7.disponibilite|date('H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_ligne7_show', {'id': ligne7.id}) }}">show</a>
                    <a href="{{ path('app_ligne7_edit', {'id': ligne7.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne7_new') }}">Create new</a>
{% endblock %}
