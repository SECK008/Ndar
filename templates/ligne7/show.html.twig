{% extends 'base.html.twig' %}

{% block title %}Ligne7{% endblock %}

{% block body %}
    <h1>Ligne7</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne7.id }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ ligne7.trajet }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ ligne7.disponibilite ? ligne7.disponibilite|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne7_index') }}">back to list</a>

    <a href="{{ path('app_ligne7_edit', {'id': ligne7.id}) }}">edit</a>

    {{ include('ligne7/_delete_form.html.twig') }}
{% endblock %}
