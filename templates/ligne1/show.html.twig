{% extends 'base.html.twig' %}

{% block title %}Ligne1{% endblock %}

{% block body %}
    <h1>Ligne1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne1.id }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ ligne1.trajet }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ ligne1.disponibilite ? ligne1.disponibilite|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne1_index') }}">back to list</a>

    <a href="{{ path('app_ligne1_edit', {'id': ligne1.id}) }}">edit</a>

    {{ include('ligne1/_delete_form.html.twig') }}
{% endblock %}
