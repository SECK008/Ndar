{% extends 'base.html.twig' %}

{% block title %}Ligne4{% endblock %}

{% block body %}
    <h1>Ligne4</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne4.id }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ ligne4.trajet }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ ligne4.disponibilite ? ligne4.disponibilite|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne4_index') }}">back to list</a>

    <a href="{{ path('app_ligne4_edit', {'id': ligne4.id}) }}">edit</a>

    {{ include('ligne4/_delete_form.html.twig') }}
{% endblock %}
