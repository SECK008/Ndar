<form method="post" action="{{ path('app_ligne4_delete', {'id': ligne4.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne4.id) }}">
    <button class="btn">Delete</button>
</form>
